---
const pathname = Astro.url.pathname.replace(/\/+$/, "") || "/";

const links = [
  { href: "/", label: "Profile", active: pathname === "/" },
  {
    href: "/projects",
    label: "Projects",
    active: pathname.startsWith("/projects"),
  },
  {
    href: "/experience",
    label: "Experience",
    active: pathname.startsWith("/experience"),
  },
  {
    href: "/certifications",
    label: "Certifications",
    active: pathname.startsWith("/certifications"),
  },
  {
    href: "/resume",
    label: "Resume",
    active: pathname.startsWith("/resume"),
  },
] as const;
---

<div class="site-nav">
  <a class="site-nav-mark" href="/" aria-label="Rehan A Ghias — home">RG</a>
  <nav class="site-nav-links" aria-label="Primary">
    {
      links.map((link) => (
        <a
          href={link.href}
          class:list={["site-nav-link", { active: link.active }]}
          aria-current={link.active ? "page" : undefined}
        >
          {link.label}
        </a>
      ))
    }
  </nav>
</div>

<style>
  .site-nav {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: 12px 16px;
    padding: 10px 0;
    width: 100%;
    background: transparent;
    border: 0;
  }

  .site-nav-mark {
    flex-shrink: 0;
    display: inline-flex;
    align-items: center;
    min-height: 44px;
    font-family: var(--font-mono);
    font-size: var(--fs-label);
    font-weight: 500;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--color-on-dark);
    text-decoration: none;
    background: transparent;
  }

  .site-nav-mark:hover {
    color: var(--color-on-dark);
    text-decoration: none;
    background: transparent;
  }

  .site-nav-links {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: flex-end;
    gap: 4px 12px;
    margin: 0;
    padding: 0;
    border: 0;
    min-width: 0;
    flex: 1 1 auto;
    background: transparent;
  }

  .site-nav-link {
    flex-shrink: 0;
    display: inline-flex;
    align-items: center;
    min-height: 44px;
    padding: 0 2px;
    font-family: var(--font-mono);
    font-size: var(--fs-label);
    font-weight: 500;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--color-on-dark-muted);
    text-decoration: none;
    background: transparent;
    border: 0;
  }

  .site-nav-link:hover {
    color: var(--color-on-dark);
    text-decoration: none;
    background: transparent;
  }

  .site-nav-link.active {
    color: var(--color-on-dark);
  }

  @media (max-width: 700px) {
    .site-nav-links {
      gap: 2px 8px;
    }

    .site-nav-link {
      letter-spacing: 0.04em;
    }
  }

  @media (max-width: 390px) {
    .site-nav-links {
      gap: 0 6px;
    }

    .site-nav-link {
      font-size: 12px;
      letter-spacing: 0.03em;
    }
  }
</style>
